# FlyRank FE-AA1 — Buttons with a Brain

A small production-style micro-interaction demonstrating idle, loading, success and error states.

## Run
`npm install && npm run dev`

## Build
`npm run build`

Includes native button semantics, `aria-busy`, an `aria-live` status region, keyboard focus and reduced-motion support.